services:
  executive_assistant:
    build: .
    restart: unless-stopped
    environment:
      EXECUTIVE_ASSISTANT_CHANNELS: ${EXECUTIVE_ASSISTANT_CHANNELS:-http}
      CHECKPOINT_STORAGE: ${CHECKPOINT_STORAGE:-postgres}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_DB: ${POSTGRES_DB:-executive_assistant_db}
      POSTGRES_USER: ${POSTGRES_USER:-executive_assistant}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-executive_assistant_password}
      TZ: ${TZ:-UTC}
    volumes:
      - ./data:/app/data
    ports:
      - "${EXECUTIVE_ASSISTANT_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:18-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-executive_assistant_password}
      POSTGRES_DB: ${POSTGRES_DB:-executive_assistant_db}
      POSTGRES_USER: ${POSTGRES_USER:-executive_assistant}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql
      - ./migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-executive_assistant}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
