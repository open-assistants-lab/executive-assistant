name: atlas-so
display: Atlas.so
icon: atlas
category: support
version: '1.0'
description: Atlas.so
auth:
  type: api_key
  api_key:
    env_var: ATLAS-SO_API_KEY
  required_fields:
  - name: apiKey
    label: API Key
    placeholder: ''
    input_type: text
    help_text: The API key for your Atlas.so account
    optional: false
setup_guide_url: https://nango.dev/docs/integrations/all/atlas-so
tool_source:
  type: mcp
  server_name: atlas-so
  command: atlas-so-mcp-server
